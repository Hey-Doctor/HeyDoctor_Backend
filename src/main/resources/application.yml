server:
  port: 8080

spring:
  config:
    import: optional:file:.env[.properties]
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql: TRACE

jwt:
  secret: ${JWT_SECRET}
  access-token:
    expiration-time: ${ACCESS_TOKEN_EXPIRATION_TIME}
  refresh-token:
    expiration-time: ${REFRESH_TOKEN_EXPIRATION_TIME}

springdoc:
  swagger-ui:
    path: /swagger-ui.html

api:
  public:
    hospital-key: Wjn354cR5FyaLyi8ljwEFz3haoZfNnwwkPqzKkSorTG/ekqLLvQ7zUv1aZdZ4axe4y0UDDq7mjEnNJPYyC6CUw==
  kakao:
    rest-key: 63e456faac2c82ccf10db8911595efc4

gemini:
  api-key: ${GEMINI_API_KEY}